import { RoutingConfigItem } from '@utils/router';
import ReduxHooksSample from '.';

const routerConfig: RoutingConfigItem = {
  path: '/redux',
  component: ReduxHooksSample,
};
export default routerConfig;
